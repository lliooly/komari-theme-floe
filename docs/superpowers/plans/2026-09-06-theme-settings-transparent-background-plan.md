# Floe 嵌入式设置页透明背景实施计划

## 实施步骤

1. 在应用壳层同步嵌入式设置页的 body data 标记，并在卸载时清理。
2. 增加透明背景 CSS，隐藏嵌入设置页的 Floe 背景图和遮罩。
3. 更新版本说明，执行 TypeScript、变更文件 ESLint、静态构建和主题包验证。
4. 提交并发布 `v0.1.4`。

## 完成标准

- 只有 `/settings?embedded=1` 使用透明页面背景。
- 设置卡片的毛玻璃效果仍由现有卡片模糊开关控制。
- `v0.1.4` 主题包可从 GitHub Release 下载。
